package GUI;

import javax.swing.*;

public class StudentMessage extends JFrame {
    private JPanel panel1;
    StudentMessage(){
        add(panel1);
        setSize(400,200);
        setTitle("Nhắc nhở");
        setLocationRelativeTo(null);
    }
}
